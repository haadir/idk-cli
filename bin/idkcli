#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/../lib/utils.sh"

main() {
  case "${1:-}" in
    hello)
      hello_command
      ;;
    *)
      echo "Usage: idkcli hello"
      ;;
  esac
}

main "$@"
